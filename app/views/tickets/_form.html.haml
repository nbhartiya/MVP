= simple_form_for(@ticket) do |f|
  = f.error_notification

  .form-inputs
    = f.input :foodie_id
    = f.input :event_id
    = f.input :purchaser_id
    = f.input :num_guests
    = f.input :guest_id

  .form-actions
    = f.button :submit
