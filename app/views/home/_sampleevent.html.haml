-#TODO: Make this image into a link to events
-if event.images.present?
  =filepicker_image_tag event.images.first.image
-else
  =link_to image_tag("Food 17.jpg")
.caption
  .caption-text
    %h4 #{event.title}
    %p #{event.description}